# System Files
*.python-version
.vscode
.DS_Store
**/.DS_Store
*.dir
.direnv
.venv

# Generated Files
*.egg-info

build/
__pycache__
*.log
*.pyc
*.bin

# transactional emulator
transactional_emulator/target
transactional_emulator/result

transactional_emulator/vendor
transactional_emulator/testbench/build/
.venv-tvm/

# Manager run artifacts (persistent hbm_bin + per-call ir dumps) — not source
managerbuild/
# Archived manager builds (1GB hbm dumps + ISA) — machine-local, never commit
SSB_build_*/

# Local-only experiments / backups (not shipped)
compiler_backup_*/
compiler_mlir/

# Session caches / snapshots / backups (large, machine-local — never commit)
transactional_emulator/testbench/.tb_cache/
transactional_emulator/testbench/.ssb_steps/
transactional_emulator/testbench/build_bak_*/
transactional_emulator/testbench/build——step0bak/
*.bak
*.bak2
plena_settings.toml.bak.*
# toml config backups/snapshots (e.g. plena_settings.SSB_build_1.toml)
plena_settings.*.toml
# loose run logs / progress snapshots
verbose_step*.txt
tvm_compiler_progress.html
transactional_emulator/testbench/report_*.txt

# Manager run artifacts for any block variant (DSB/SSB) — 1GB+ hbm dumps, machine-local
managerbuild_*/
# Local Claude Code session/worktree state
.claude/
