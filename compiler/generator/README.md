# Dependencies

You only need to install `uv` (https://docs.astral.sh/uv/getting-started/installation/)

For faster run, you may want to install `just`(https://github.com/casey/just).

# To run

```bash
just run
```